clear all 
close all 

% Creating an empty 1024x1024 image
SIZE = [1024, 1024];
I = initializeImage(SIZE, 2);
imshow(I)

N_particles = 1; 
max_speed = 1; 
SN_ratio = 1; 

function I = initializeImage(size, N)
% image: the image to which we want to add particles 
    I = 255*ones(size);
    positions = randi([0, 1024], )
end